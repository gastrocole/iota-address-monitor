import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iam.mobile',
  appName: 'iota-address-monitor',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
